# 1. Create the `podman` zone:
sudo firewall-cmd --permanent --new-zone=podman

# 2. Add the Tailscale interface to the `podman` zone:
sudo firewall-cmd --permanent --zone=podman --add-interface=tailscale0

# 3. Allow all traffic within the `podman` zone:
sudo firewall-cmd --permanent --zone=podman --set-target=ACCEPT

# 4. Reload `firewalld`:
sudo firewall-cmd --reload

# 5. (Optional) Verify the configuration:
sudo firewall-cmd --list-all --zone=podman
